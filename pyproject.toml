[project]
name = "pi-home-server"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "pip>=25.3",
    "ansible-lint>=25.12.2",
    "molecule",
    "molecule-docker",
    "yamllint>=1.37.1",
]
